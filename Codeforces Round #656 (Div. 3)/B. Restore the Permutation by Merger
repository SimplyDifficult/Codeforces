#include <stdio.h>
#include <bits/stdc++.h> 

using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        int a[2*n];
        unordered_map<int,int>map;
        for(int i=0;i<2*n;i++){
            cin>>a[i];
            if(map.find(a[i])==map.end()){
                map.insert(make_pair(a[i],1));
                cout<<a[i]<<" ";
            }
        }
        cout<<endl;
    }

    return 0;
}
